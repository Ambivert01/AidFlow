# Deploy with: npx hardhat run scripts/deploy.js --network localhost
# (after starting a local node in a separate terminal: npx hardhat node)

PRIVATE_KEY=

RPC_URL=

AUDIT_CONTRACT_ADDRESS=

ETHERSCAN_API_KEY=
