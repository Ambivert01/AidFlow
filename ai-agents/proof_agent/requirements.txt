fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
pytesseract==0.3.10
Pillow==10.1.0
imagehash==4.3.1
requests==2.31.0
python-dotenv==1.0.0
pymongo==4.6.1
boto3==1.34.0

# IMPORTANT - SYSTEM DEPENDENCY NOT INSTALLABLE VIA PIP:
# pytesseract is a Python wrapper around the Tesseract OCR binary, which
# must be installed separately on the host OS, e.g.:
#   Ubuntu/Debian: sudo apt-get install -y tesseract-ocr
#   macOS:         brew install tesseract
#   Windows:       https://github.com/UB-Mannheim/tesseract/wiki
# Without it, OCR validation will fail gracefully (returns success=False,
# flags the proof as OCR_FAILED) rather than crashing the agent, but receipt
# amount/vendor verification will not work until this is installed.
